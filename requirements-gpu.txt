# Optional training-only GPU stack.
# Install after choosing the correct PyTorch CUDA wheel for your NVIDIA driver.
#
# Recommended install flow on Windows/NVIDIA:
#   python -m pip install --upgrade pip
#   python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install -r requirements-gpu.txt
#
# The runtime optimizer does not require these packages.
sentence-transformers
easyocr
Pillow
pytesseract
