from __future__ import annotations

import argparse
import importlib.util
import json
import os
import queue
import subprocess
import sys
import tempfile
import threading
import time
from collections import Counter, deque
from pathlib import Path
from typing import Any

try:
    import tkinter as tk
except Exception:
    tk = None

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from tools import run_adversarial_battle as battle


OUT_DIR = ROOT / "training_outputs"
LIVE_ROUNDS = OUT_DIR / "live_adversarial_battle_rounds.jsonl"
LIVE_SUMMARY = OUT_DIR / "live_adversarial_battle_summary.json"
LIVE_LEARNING_STATE = OUT_DIR / "live_adversarial_learning_state.json"
LIVE_EVAL_INPUT = OUT_DIR / ".live_adversarial_eval_input.jsonl"
LIVE_EVAL_OUTPUT = OUT_DIR / ".live_adversarial_eval_output.json"


def _json_default(value: Any) -> str:
    return str(value)


def _atomic_write_json(path: Path, payload: dict[str, Any]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    fd, tmp_name = tempfile.mkstemp(prefix=f".{path.name}.", suffix=".tmp", dir=str(path.parent))
    tmp = Path(tmp_name)
    try:
        with os.fdopen(fd, "w", encoding="utf-8", newline="\n") as handle:
            json.dump(payload, handle, ensure_ascii=False, indent=2, sort_keys=True, default=_json_default)
            handle.write("\n")
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(tmp, path)
    finally:
        tmp.unlink(missing_ok=True)


def _atomic_write_jsonl(path: Path, rows: list[dict[str, Any]]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    fd, tmp_name = tempfile.mkstemp(prefix=f".{path.name}.", suffix=".tmp", dir=str(path.parent))
    tmp = Path(tmp_name)
    try:
        with os.fdopen(fd, "w", encoding="utf-8", newline="\n") as handle:
            for row in rows:
                handle.write(json.dumps(row, ensure_ascii=False, sort_keys=True, default=_json_default) + "\n")
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(tmp, path)
    finally:
        tmp.unlink(missing_ok=True)


def _append_jsonl_atomic(path: Path, row: dict[str, Any]) -> None:
    rows = battle._read_jsonl(path)
    rows.append(row)
    _atomic_write_jsonl(path, rows)


def _evaluate_cases_with_tool_python(
    cases: list[dict[str, Any]],
    batch_size: int,
    cpu_workers: int,
    tool_python: str,
) -> tuple[list[dict[str, Any]], float]:
    tool_path = Path(tool_python)
    if not tool_path.exists() or tool_path.resolve() == Path(sys.executable).resolve():
        return battle._evaluate_cases(cases, batch_size, cpu_workers)

    LIVE_EVAL_INPUT.unlink(missing_ok=True)
    LIVE_EVAL_OUTPUT.unlink(missing_ok=True)
    _atomic_write_jsonl(LIVE_EVAL_INPUT, cases)
    started = time.perf_counter()
    proc = subprocess.run(
        [
            str(tool_path),
            str(Path(__file__).resolve()),
            "--evaluate-cases-only",
            str(LIVE_EVAL_INPUT),
            str(LIVE_EVAL_OUTPUT),
            "--batch-size",
            str(batch_size),
            "--cpu-workers",
            str(cpu_workers),
        ],
        cwd=ROOT,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
    )
    elapsed = time.perf_counter() - started
    if proc.returncode == 0 and LIVE_EVAL_OUTPUT.exists():
        payload = battle._read_json(LIVE_EVAL_OUTPUT)
        results = payload.get("results")
        if isinstance(results, list):
            LIVE_EVAL_INPUT.unlink(missing_ok=True)
            LIVE_EVAL_OUTPUT.unlink(missing_ok=True)
            return [row for row in results if isinstance(row, dict)], elapsed

    results, fallback_elapsed = battle._evaluate_cases(cases, batch_size, cpu_workers)
    for result in results:
        if result.get("severity") == "error":
            result["suggested_optimizer_rule"] = (
                f"{result.get('suggested_optimizer_rule', '')}; delegated venv evaluation failed "
                f"with return code {proc.returncode}: {proc.stdout[-500:]}"
            )
    return results, elapsed + fallback_elapsed


def _torch_status() -> dict[str, Any]:
    status: dict[str, Any] = {
        "torch_installed": False,
        "cuda_available": False,
        "gpu_name": None,
        "torch_error": None,
    }
    try:
        import torch
    except Exception as exc:
        status["torch_error"] = f"{type(exc).__name__}: {exc}"
        return status

    status["torch_installed"] = True
    try:
        status["cuda_available"] = bool(torch.cuda.is_available())
        if status["cuda_available"]:
            status["gpu_name"] = torch.cuda.get_device_name(0)
    except Exception as exc:
        status["torch_error"] = f"{type(exc).__name__}: {exc}"
        status["cuda_available"] = False
    return status


def _evaluation_mode(args: argparse.Namespace, torch_status: dict[str, Any]) -> dict[str, Any]:
    workers = max(1, int(args.cpu_workers))
    cpu_mode = "cpu_threaded" if workers > 1 else "cpu_serial"
    if not args.use_gpu:
        return {
            "actual_evaluation_mode": cpu_mode,
            "gpu_backend_used": False,
            "cpu_only_reason": "GPU evaluation was not requested.",
        }
    if not torch_status.get("torch_installed"):
        return {
            "actual_evaluation_mode": cpu_mode,
            "gpu_backend_used": False,
            "cpu_only_reason": f"PyTorch is not importable: {torch_status.get('torch_error') or 'unknown import error'}",
        }
    if not torch_status.get("cuda_available"):
        return {
            "actual_evaluation_mode": cpu_mode,
            "gpu_backend_used": False,
            "cpu_only_reason": "CUDA is unavailable; whole-profile optimizer damage evaluation ran on CPU.",
        }
    return {
        "actual_evaluation_mode": "gpu_batch_ready",
        "gpu_backend_used": False,
        "cpu_only_reason": "",
    }


def _print_startup(torch_status: dict[str, Any], mode: dict[str, Any]) -> None:
    print(f"torch installed: {torch_status['torch_installed']}", flush=True)
    print(f"cuda available: {torch_status['cuda_available']}", flush=True)
    print(f"GPU name: {torch_status.get('gpu_name') or 'none'}", flush=True)
    print(f"actual_evaluation_mode: {mode['actual_evaluation_mode']}", flush=True)
    print(f"cpu_only_reason: {mode.get('cpu_only_reason') or ''}", flush=True)


class RunControl:
    def __init__(self, args: argparse.Namespace) -> None:
        self.mode = str(args.run_mode or "rounds")
        self.minutes = float(args.minutes or 0.0)
        self.stop_after_round = threading.Event()
        self.stop_now = threading.Event()
        self._lock = threading.Lock()
        self._deadline: float | None = None
        if self.mode == "timed":
            self._deadline = time.time() + max(0.01, self.minutes) * 60.0

    def add_minutes(self, minutes: float) -> None:
        with self._lock:
            if self._deadline is None:
                self._deadline = time.time()
            self._deadline += float(minutes) * 60.0

    def request_stop_after_round(self) -> None:
        self.stop_after_round.set()

    def request_stop_now(self) -> None:
        self.stop_now.set()
        self.stop_after_round.set()

    def end_early(self) -> None:
        self.request_stop_after_round()

    def time_remaining(self) -> float | None:
        with self._lock:
            if self._deadline is None:
                return None
            return max(0.0, self._deadline - time.time())

    def should_start_round(self, completed_rounds: int, requested_rounds: int) -> bool:
        if self.stop_now.is_set() or self.stop_after_round.is_set():
            return False
        if self.mode == "endless":
            return True
        if self.mode == "timed":
            remaining = self.time_remaining()
            return remaining is None or remaining > 0
        return completed_rounds < max(0, int(requested_rounds))


def _format_seconds(seconds: float | None) -> str:
    if seconds is None:
        return "n/a"
    seconds = max(0, int(seconds))
    h = seconds // 3600
    m = (seconds % 3600) // 60
    s = seconds % 60
    if h:
        return f"{h}h {m}m {s}s"
    return f"{m}m {s}s"


def _learning_status(history: list[float]) -> str:
    if len(history) < 4:
        return "Learning: warming up"
    recent = history[-4:]
    delta = recent[-1] - recent[0]
    if delta > 0.015:
        return "Learning: improving"
    if delta < -0.015:
        return "Learning: regressing"
    return "Learning: plateauing"



def _load_learning_state() -> dict[str, Any]:
    if not LIVE_LEARNING_STATE.exists():
        return {
            "version": 1,
            "rounds_seen": 0,
            "history": [],
            "categories": {},
            "optimizer_level": 1.0,
            "anti_optimizer_level": 1.0,
            "last_status": "Learning: warming up",
            "adaptive_focus": [],
        }
    try:
        data = json.loads(LIVE_LEARNING_STATE.read_text(encoding="utf-8"))
        return data if isinstance(data, dict) else {}
    except Exception:
        return {
            "version": 1,
            "rounds_seen": 0,
            "history": [],
            "categories": {},
            "optimizer_level": 1.0,
            "anti_optimizer_level": 1.0,
            "last_status": "Learning: recovering state",
            "adaptive_focus": [],
        }


def _write_learning_state(state: dict[str, Any]) -> None:
    _atomic_write_json(LIVE_LEARNING_STATE, state)


def _moving_average(values: list[float], window: int = 5) -> float:
    if not values:
        return 0.0
    tail = values[-max(1, window):]
    return sum(tail) / len(tail)


def _learning_public_view(state: dict[str, Any]) -> dict[str, Any]:
    history = state.get("history", []) or []
    cats = state.get("categories", {}) or {}
    ranked = sorted(
        cats.items(),
        key=lambda item: (
            float(item[1].get("anti_success_rate", 0.0)),
            int(item[1].get("failures", 0)),
        ),
        reverse=True,
    )
    return {
        "rounds_seen": int(state.get("rounds_seen", 0) or 0),
        "optimizer_level": round(float(state.get("optimizer_level", 1.0) or 1.0), 4),
        "anti_optimizer_level": round(float(state.get("anti_optimizer_level", 1.0) or 1.0), 4),
        "status": str(state.get("last_status", "Learning: warming up")),
        "adaptive_focus": [name for name, _ in ranked[:5]],
        "accuracy_history": [round(float(row.get("accuracy", 0.0)), 6) for row in history[-80:]],
        "anti_success_history": [round(float(row.get("failure_rate", 0.0)), 6) for row in history[-80:]],
        "moving_accuracy": round(_moving_average([float(row.get("accuracy", 0.0)) for row in history], 5), 6),
        "moving_anti_success": round(_moving_average([float(row.get("failure_rate", 0.0)) for row in history], 5), 6),
        "worst_categories": [
            {
                "category": name,
                "failures": int(info.get("failures", 0) or 0),
                "passes": int(info.get("passes", 0) or 0),
                "anti_success_rate": round(float(info.get("anti_success_rate", 0.0) or 0.0), 4),
                "weight": round(float(info.get("weight", 1.0) or 1.0), 4),
            }
            for name, info in ranked[:8]
        ],
    }


def _update_learning_state(payload: dict[str, Any], control: Any | None = None) -> dict[str, Any]:
    state = _load_learning_state()
    history = list(state.get("history", []) or [])
    cats = dict(state.get("categories", {}) or {})

    tested = max(1, int(payload.get("profiles_tested", 0) or 0))
    passed = int(payload.get("pass_count", 0) or 0)
    failed = int(payload.get("fail_count", 0) or 0)
    accuracy = passed / tested
    failure_rate = failed / tested

    row = {
        "round": int(payload.get("round", len(history) + 1) or len(history) + 1),
        "accuracy": accuracy,
        "failure_rate": failure_rate,
        "pass_count": passed,
        "fail_count": failed,
        "top_fail": str(payload.get("top_fail", "none")),
        "gpu_rows_scored": int(payload.get("gpu_rows_scored", 0) or 0),
        "created_at": time.time(),
    }
    history.append(row)
    history = history[-300:]

    failure_categories = payload.get("failure_categories") or {}
    for category, count in failure_categories.items():
        name = str(category)
        info = dict(cats.get(name, {}))
        info["failures"] = int(info.get("failures", 0) or 0) + int(count)
        info["last_seen_round"] = row["round"]
        cats[name] = info

    # Any category not failing this round gets small pass credit. This lets the
    # graph move instead of acting like a static logger.
    for name, info in list(cats.items()):
        if name not in failure_categories:
            info["passes"] = int(info.get("passes", 0) or 0) + max(1, passed // max(1, len(cats)))
            cats[name] = info

    for name, info in list(cats.items()):
        f = int(info.get("failures", 0) or 0)
        p = int(info.get("passes", 0) or 0)
        total = max(1, f + p)
        rate = f / total
        info["anti_success_rate"] = rate
        # Weight drives adaptive case ordering. Weak spots get attacked more.
        info["weight"] = max(1.0, min(10.0, 1.0 + rate * 6.0 + min(3.0, f / 250.0)))
        cats[name] = info

    accuracies = [float(item.get("accuracy", 0.0)) for item in history]
    if len(accuracies) >= 6:
        old = sum(accuracies[-6:-3]) / 3
        new = sum(accuracies[-3:]) / 3
        delta = new - old
        if delta > 0.01:
            status = "Learning: optimizer improving"
        elif delta < -0.01:
            status = "Learning: anti-optimizer landing blows"
        else:
            status = "Learning: plateauing"
    else:
        status = "Learning: warming up"

    optimizer_level = max(1.0, float(state.get("optimizer_level", 1.0) or 1.0) + max(0.0, accuracy - 0.45) * 0.1 - max(0.0, failure_rate - 0.55) * 0.05)
    anti_level = max(1.0, float(state.get("anti_optimizer_level", 1.0) or 1.0) + max(0.0, failure_rate - 0.45) * 0.1)

    ranked = sorted(
        cats.items(),
        key=lambda item: (float(item[1].get("weight", 1.0)), int(item[1].get("failures", 0))),
        reverse=True,
    )

    state.update(
        {
            "version": 1,
            "rounds_seen": len(history),
            "history": history,
            "categories": cats,
            "optimizer_level": optimizer_level,
            "anti_optimizer_level": anti_level,
            "last_status": status,
            "adaptive_focus": [name for name, _ in ranked[:5]],
            "updated_at": time.time(),
        }
    )
    _write_learning_state(state)
    return state


def _apply_live_adaptation(cases: list[dict[str, Any]], learning_state: dict[str, Any], max_count: int) -> list[dict[str, Any]]:
    """Make later rounds attack known weak spots while preserving controls."""
    if not cases:
        return cases
    cats = learning_state.get("categories", {}) or {}
    if not cats:
        return cases[:max_count]

    def weight(case: dict[str, Any]) -> tuple[float, str]:
        category = battle._normalize_category(case.get("category"))
        info = cats.get(category, {})
        base = float(info.get("weight", 1.0) or 1.0)
        # Keep positive controls alive so "accuracy" has both hits and misses.
        if str(case.get("trap_type")) == "positive_control":
            base += 1.5
        return (base, str(case.get("case_id", "")))

    ranked = sorted(cases, key=weight, reverse=True)
    keep = ranked[:max_count]

    # Force at least 10 percent positive controls when available. Without this,
    # the graph becomes a flat failure logger instead of a duel.
    positives = [case for case in cases if str(case.get("trap_type")) == "positive_control"]
    if positives and keep:
        desired = max(1, int(max_count * 0.10))
        current = sum(1 for case in keep if str(case.get("trap_type")) == "positive_control")
        if current < desired:
            keep = keep[: max(0, max_count - (desired - current))] + positives[: desired - current]
    keep.sort(key=lambda row: str(row.get("case_id", "")))
    return keep[:max_count]


class _LiveAdversarialBattleDashboard:
    BG = "#0b1020"
    PANEL = "#111827"
    PANEL2 = "#1f2937"
    TEXT = "#e5e7eb"
    MUTED = "#9ca3af"
    GOOD = "#22c55e"
    BAD = "#ef4444"
    WARN = "#f59e0b"
    BLUE = "#38bdf8"
    PURPLE = "#a78bfa"

    def __init__(self, control: RunControl) -> None:
        if tk is None:
            raise RuntimeError("tkinter is not available")
        self.control = control
        self.root = tk.Tk()
        self.root.title("Optimizer vs Anti-Optimizer Duel")
        self.root.geometry("1200x820")
        self.root.configure(bg=self.BG)
        self.root.protocol("WM_DELETE_WINDOW", self._on_close)

        self.queue: queue.Queue[dict[str, Any]] = queue.Queue()
        self.history: list[float] = []
        self.fail_history: list[float] = []
        self.last_payload: dict[str, Any] = {}
        self.done = False
        self.worker: threading.Thread | None = None
        self.cards: dict[str, tk.Label] = {}

        title = tk.Label(
            self.root,
            text="OPTIMIZER vs ANTI-OPTIMIZER LIVE DUEL",
            bg=self.BG,
            fg=self.TEXT,
            font=("Segoe UI", 18, "bold"),
            anchor="w",
        )
        title.pack(fill="x", padx=14, pady=(12, 2))

        self.status = tk.Label(
            self.root,
            text="Starting...",
            bg=self.BG,
            fg=self.BLUE,
            font=("Segoe UI", 11, "bold"),
            anchor="w",
        )
        self.status.pack(fill="x", padx=14, pady=(0, 8))

        controls = tk.Frame(self.root, bg=self.BG)
        controls.pack(fill="x", padx=12, pady=6)

        self._button(controls, "Stop after round", self.control.request_stop_after_round, self.WARN).pack(side="left", padx=4)
        self._button(controls, "Stop now", self.control.request_stop_now, self.BAD).pack(side="left", padx=4)
        self._button(controls, "End early", self.control.end_early, self.WARN).pack(side="left", padx=4)
        self._button(controls, "+5 min", lambda: self.control.add_minutes(5), self.BLUE).pack(side="left", padx=4)
        self._button(controls, "+15 min", lambda: self.control.add_minutes(15), self.BLUE).pack(side="left", padx=4)
        self._button(controls, "+30 min", lambda: self.control.add_minutes(30), self.BLUE).pack(side="left", padx=4)
        self._button(controls, "+1 hour", lambda: self.control.add_minutes(60), self.BLUE).pack(side="left", padx=4)

        card_frame = tk.Frame(self.root, bg=self.BG)
        card_frame.pack(fill="x", padx=12, pady=8)

        for key in ["Accuracy", "Anti hit rate", "GPU rows", "Optimizer level", "Anti level", "Learning state"]:
            self.cards[key] = self._card(card_frame, key, "--")
            self.cards[key].pack(side="left", padx=5, fill="x", expand=True)

        main = tk.Frame(self.root, bg=self.BG)
        main.pack(fill="both", expand=True, padx=12, pady=8)

        left = tk.Frame(main, bg=self.PANEL)
        left.pack(side="left", fill="both", expand=True, padx=(0, 6))

        right = tk.Frame(main, bg=self.PANEL)
        right.pack(side="right", fill="both", expand=True, padx=(6, 0))

        tk.Label(left, text="Duel intelligence graph", bg=self.PANEL, fg=self.TEXT, font=("Segoe UI", 13, "bold")).pack(anchor="w", padx=10, pady=(10, 2))
        tk.Label(left, text="Green = optimizer accuracy | Red = anti-optimizer success | Blue = moving accuracy", bg=self.PANEL, fg=self.MUTED, font=("Segoe UI", 9)).pack(anchor="w", padx=10)

        self.canvas = tk.Canvas(left, width=700, height=390, bg="#030712", highlightthickness=0)
        self.canvas.pack(fill="both", expand=True, padx=10, pady=10)

        self.log = tk.Text(left, height=8, bg="#030712", fg=self.TEXT, insertbackground=self.TEXT, font=("Consolas", 9), relief="flat")
        self.log.pack(fill="x", padx=10, pady=(0, 10))

        tk.Label(right, text="Current stats", bg=self.PANEL, fg=self.TEXT, font=("Segoe UI", 13, "bold")).pack(anchor="w", padx=10, pady=(10, 4))
        self.stats_text = tk.Text(right, height=14, bg="#030712", fg=self.TEXT, font=("Consolas", 9), relief="flat")
        self.stats_text.pack(fill="x", padx=10, pady=6)

        tk.Label(right, text="Worst failure categories", bg=self.PANEL, fg=self.TEXT, font=("Segoe UI", 13, "bold")).pack(anchor="w", padx=10, pady=(10, 4))
        self.fail_text = tk.Text(right, height=12, bg="#030712", fg=self.TEXT, font=("Consolas", 9), relief="flat")
        self.fail_text.pack(fill="both", expand=True, padx=10, pady=(0, 10))

        self.root.after(100, self._tick)

    def _button(self, parent: Any, text: str, command: Any, color: str) -> tk.Button:
        return tk.Button(
            parent,
            text=text,
            command=command,
            bg=color,
            fg="#020617",
            activebackground=color,
            activeforeground="#020617",
            relief="flat",
            font=("Segoe UI", 9, "bold"),
            padx=10,
            pady=5,
        )

    def _card(self, parent: Any, title: str, value: str) -> tk.Label:
        label = tk.Label(
            parent,
            text=f"{title}
{value}",
            bg=self.PANEL2,
            fg=self.TEXT,
            font=("Segoe UI", 10, "bold"),
            padx=10,
            pady=10,
            justify="center",
            relief="flat",
        )
        return label

    def _on_close(self) -> None:
        self.control.request_stop_after_round()
        try:
            self.status.config(text="Close requested. Stopping after current round...")
        except Exception:
            pass

    def push(self, payload: dict[str, Any]) -> None:
        self.queue.put(payload)

    def start(self, worker: threading.Thread) -> None:
        self.worker = worker
        self.root.mainloop()

    def _tick(self) -> None:
        try:
            while True:
                payload = self.queue.get_nowait()
                if payload.get("__done__"):
                    self.done = True
                    self.status.config(text="Done. Review summary, then close window.", fg=self.GOOD)
                    continue
                self._update_payload(payload)
        except queue.Empty:
            pass

        if not self.done:
            if self.control.stop_after_round.is_set():
                self.status.config(text="Stop requested. Finishing current round cleanly...", fg=self.WARN)
            else:
                self.status.config(text="Running adaptive duel", fg=self.BLUE)
        try:
            self.root.after(250, self._tick)
        except Exception:
            pass

    def _update_payload(self, payload: dict[str, Any]) -> None:
        self.last_payload = payload
        tested = int(payload.get("profiles_tested", 0) or 0)
        passed = int(payload.get("pass_count", 0) or 0)
        failed = int(payload.get("fail_count", 0) or 0)
        accuracy = passed / max(1, tested)
        fail_rate = failed / max(1, tested)
        self.history.append(accuracy)
        self.fail_history.append(fail_rate)
        self.history = self.history[-240:]
        self.fail_history = self.fail_history[-240:]

        learning = payload.get("learning_state") or {}
        status = learning.get("status") or _learning_status(self.history)
        optimizer_level = learning.get("optimizer_level", 1.0)
        anti_level = learning.get("anti_optimizer_level", 1.0)

        card_values = {
            "Accuracy": f"{accuracy * 100:.1f}%",
            "Anti hit rate": f"{fail_rate * 100:.1f}%",
            "GPU rows": str(payload.get("gpu_rows_scored", 0)),
            "Optimizer level": str(optimizer_level),
            "Anti level": str(anti_level),
            "Learning state": str(status).replace("Learning: ", ""),
        }
        for key, value in card_values.items():
            self.cards[key].config(text=f"{key}
{value}")

        self.stats_text.delete("1.0", "end")
        lines = [
            f"Run mode:        {payload.get('run_mode')}",
            f"Time remaining:  {_format_seconds(self.control.time_remaining())}",
            f"Round:           {payload.get('round')}/{payload.get('rounds_requested')}",
            f"Profiles tested: {tested}",
            f"Pass / Fail:     {passed} / {failed}",
            f"Wall p/s:        {payload.get('profiles_per_sec_wall')}",
            f"Score p/s:       {payload.get('profiles_per_sec_scoring_only')}",
            f"GPU submitted:   {payload.get('gpu_rows_submitted')}",
            f"GPU scored:      {payload.get('gpu_rows_scored')}",
            f"GPU idle:        {payload.get('gpu_idle_estimate')}",
            f"Eval mode:       {payload.get('actual_evaluation_mode')}",
            f"Top failure:     {payload.get('top_fail')}",
            f"Bottleneck:      {payload.get('next_perf_bottleneck')}",
            f"Adaptive focus:  {', '.join(learning.get('adaptive_focus', [])[:3]) if learning else 'n/a'}",
        ]
        self.stats_text.insert("end", "
".join(lines))

        self.fail_text.delete("1.0", "end")
        failures = payload.get("failure_categories") or {}
        ranked = sorted(failures.items(), key=lambda item: int(item[1]), reverse=True)
        worst_from_learning = learning.get("worst_categories", []) if learning else []
        if ranked:
            self.fail_text.insert("end", "This round:
")
            for index, (name, count) in enumerate(ranked[:8], start=1):
                self.fail_text.insert("end", f"{index}. {name}: {count}
")
        if worst_from_learning:
            self.fail_text.insert("end", "
Persistent memory:
")
            for index, row in enumerate(worst_from_learning[:8], start=1):
                self.fail_text.insert(
                    "end",
                    f"{index}. {row.get('category')}: fail={row.get('failures')} pass={row.get('passes')} weight={row.get('weight')}
",
                )

        self.log.insert("end", f"Round {payload.get('round')}: optimizer {accuracy*100:.1f}% vs anti {fail_rate*100:.1f}% | {status}
")
        self.log.see("end")
        self._draw_graph(learning)

    def _draw_graph(self, learning: dict[str, Any]) -> None:
        canvas = self.canvas
        canvas.delete("all")
        w = max(520, int(canvas.winfo_width() or 700))
        h = max(300, int(canvas.winfo_height() or 390))
        pad = 45

        canvas.create_rectangle(0, 0, w, h, fill="#030712", outline="#030712")
        for frac in [0, 0.25, 0.5, 0.75, 1.0]:
            y = h - pad - (h - 2 * pad) * frac
            canvas.create_line(pad, y, w - pad, y, fill="#1f2937")
            canvas.create_text(20, y, text=f"{int(frac*100)}%", fill=self.MUTED, font=("Segoe UI", 8))

        canvas.create_line(pad, h - pad, w - pad, h - pad, fill="#475569")
        canvas.create_line(pad, pad, pad, h - pad, fill="#475569")

        acc = list(self.history)
        anti = list(self.fail_history)
        if len(acc) < 2:
            canvas.create_text(w // 2, h // 2, text="Waiting for more rounds...", fill=self.TEXT, font=("Segoe UI", 12, "bold"))
            return

        def points(data: list[float]) -> list[float]:
            pts: list[float] = []
            n = len(data)
            for i, value in enumerate(data):
                x = pad + (w - 2 * pad) * (i / max(1, n - 1))
                y = h - pad - (h - 2 * pad) * max(0.0, min(1.0, value))
                pts.extend([x, y])
            return pts

        def moving(data: list[float], window: int = 5) -> list[float]:
            out: list[float] = []
            for i in range(len(data)):
                chunk = data[max(0, i - window + 1): i + 1]
                out.append(sum(chunk) / len(chunk))
            return out

        canvas.create_line(*points(acc), fill=self.GOOD, width=2)
        canvas.create_line(*points(anti), fill=self.BAD, width=2)
        canvas.create_line(*points(moving(acc)), fill=self.BLUE, width=3, dash=(5, 3))

        latest_acc = acc[-1] * 100
        latest_anti = anti[-1] * 100
        status = learning.get("status") if learning else _learning_status(acc)
        canvas.create_text(w // 2, 20, text=f"{status} | optimizer {latest_acc:.1f}% | anti {latest_anti:.1f}%", fill=self.TEXT, font=("Segoe UI", 11, "bold"))
        canvas.create_text(w - 190, h - 18, text="Green=optimizer  Red=anti  Blue=moving avg", fill=self.MUTED, font=("Segoe UI", 9))


def _create_dashboard(control: RunControl) -> Any:
    if tk is None:
        return None
    try:
        return _LiveAdversarialBattleDashboard(control)
    except Exception as exc:
        print(f"dashboard initialization failed: {type(exc).__name__}: {exc}", flush=True)
        return None


def _import_status(module: str) -> str:
    return "Yes" if importlib.util.find_spec(module) else "NOT_FOUND"


def _debug_routing(args: argparse.Namespace) -> int:
    torch_status = _torch_status()
    mode = _evaluation_mode(args, torch_status)
    delegated = Path(battle._python_for_subprocesses()).exists() and Path(battle._python_for_subprocesses()).resolve() != Path(sys.executable).resolve()
    rejection = mode.get("cpu_only_reason") or "none"
    print(f"torch_installed: {torch_status['torch_installed']} | cuda_available: {torch_status['cuda_available']} | gpu_name: {torch_status.get('gpu_name') or 'none'} | raw_use_gpu_flag_value: {args.use_gpu}")
    print(f"selected_eval_backend: {'gpu_batch' if mode['actual_evaluation_mode'] == 'gpu_batch_ready' else 'cpu_threadpool'} | selected_actual_evaluation_mode: {mode['actual_evaluation_mode']}")
    print(f"gpu_scoring_import_status: {_import_status('tools.gpu_scoring')} | gpu_batch_engine_import_status: {_import_status('optimizer.gpu_batch_engine')} | profile_to_matrix_adapter_exists: {_import_status('optimizer.profile_to_matrix_adapter') == 'Yes'}")
    print(f"cpu_evaluator_selected: {not mode['gpu_backend_used']} | subprocess_or_venv_delegation_used: {delegated}")
    print(f"gpu_rejection_reason: {rejection} | cpu_underutilization_reason: synchronous_training_subprocess_or_threadpool_starvation")
    return 0


def _top_fail(failures: list[dict[str, Any]]) -> str:
    if not failures:
        return "none"
    counts = Counter(battle._normalize_category(row.get("category")) for row in failures)
    return counts.most_common(1)[0][0]


def _round_label(args: argparse.Namespace) -> str:
    if args.run_mode == "endless":
        return "endless"
    if args.run_mode == "timed":
        return f"timed:{args.minutes:g}m"
    return str(int(args.rounds))


def _round_payload(
    round_no: int,
    args: argparse.Namespace,
    cases: list[dict[str, Any]],
    results: list[dict[str, Any]],
    cpu_generation_time: float,
    scoring_time: float,
    round_wall_time: float,
    new_cases_added: int,
    mode: dict[str, Any],
    training_run: dict[str, Any],
    gpu_stats: dict[str, Any] | None = None,
) -> dict[str, Any]:
    failures = [row for row in results if not row.get("passed")]
    tested = len(results)
    wall_p_s = tested / round_wall_time if round_wall_time > 0 else 0.0
    score_p_s = tested / scoring_time if scoring_time > 0 else 0.0
    failure_categories = Counter(battle._normalize_category(row.get("category")) for row in failures)
    severity_counts = Counter(str(row.get("severity") or "unknown") for row in failures)
    target_reached = wall_p_s >= float(args.target_profiles_per_sec)
    gpu_stats = gpu_stats or {}
    gpu_rows = int(gpu_stats.get("gpu_rows_scored", 0) or 0)
    gpu_batches = int(gpu_stats.get("gpu_batches_scored", 0) or 0)
    actual_mode = "gpu_batch" if gpu_rows > 0 else ("cpu_threaded" if int(args.cpu_workers) > 1 else "cpu_serial")
    gpu_idle = 0.0 if gpu_rows > 0 else (1.0 if args.use_gpu else 0.0)
    return {
        "round": round_no,
        "rounds_requested": _round_label(args),
        "run_mode": str(args.run_mode),
        "minutes": float(args.minutes or 0.0),
        "profiles_tested": tested,
        "pass_count": tested - len(failures),
        "fail_count": len(failures),
        "profiles_per_sec_wall": round(wall_p_s, 6),
        "profiles_per_sec_scoring_only": round(score_p_s, 6),
        "target_profiles_per_sec": float(args.target_profiles_per_sec),
        "target_reached": target_reached,
        "cpu_generation_time": round(cpu_generation_time, 6),
        "scoring_time": round(scoring_time, 6),
        "round_wall_time": round(round_wall_time, 6),
        "gpu_rows_submitted": int(gpu_stats.get("gpu_rows_submitted", 0) or 0),
        "gpu_rows_scored": gpu_rows,
        "gpu_batches_scored": gpu_batches,
        "gpu_seconds": round(float(gpu_stats.get("gpu_seconds", 0.0) or 0.0), 6),
        "gpu_idle_estimate": gpu_idle,
        "new_cases_added": new_cases_added,
        "failure_categories": dict(sorted(failure_categories.items())),
        "severity_counts": dict(sorted(severity_counts.items())),
        "top_fail": _top_fail(failures),
        "top_rules_to_fix": battle._top_rules(failures),
        "next_perf_bottleneck": battle._bottleneck(
            cpu_generation_time,
            scoring_time,
            bool(args.use_gpu),
            gpu_rows,
            target_reached,
        ),
        "actual_evaluation_mode": actual_mode,
        "gpu_backend_used": gpu_rows > 0,
        "cpu_only_reason": "" if gpu_rows > 0 else mode.get("cpu_only_reason"),
        "parity_checked": bool(gpu_stats.get("parity_checked")),
        "parity_passed": bool(gpu_stats.get("parity_passed")),
        "training_subprocess": training_run,
    }


def _summary(
    args: argparse.Namespace,
    round_rows: list[dict[str, Any]],
    torch_status: dict[str, Any],
    mode: dict[str, Any],
    fatal_error: str | None = None,
) -> dict[str, Any]:
    completed = len(round_rows)
    total_profiles = sum(int(row.get("profiles_tested", 0)) for row in round_rows)
    wall_speeds = [float(row.get("profiles_per_sec_wall", 0.0)) for row in round_rows]
    score_times = sum(float(row.get("scoring_time", 0.0)) for row in round_rows)
    failure_categories: Counter[str] = Counter()
    severity_counts: Counter[str] = Counter()
    rules: Counter[str] = Counter()
    for row in round_rows:
        failure_categories.update({str(k): int(v) for k, v in (row.get("failure_categories") or {}).items()})
        severity_counts.update({str(k): int(v) for k, v in (row.get("severity_counts") or {}).items()})
        for rule in row.get("top_rules_to_fix") or []:
            rules[str(rule)] += 1

    gpu_rows = sum(int(row.get("gpu_rows_scored", 0)) for row in round_rows)
    return {
        "run_mode": str(args.run_mode),
        "minutes": float(args.minutes or 0.0),
        "rounds_requested": _round_label(args),
        "rounds_completed": completed,
        "stopped_early": bool(fatal_error) or (args.run_mode == "rounds" and completed < int(args.rounds)),
        "fatal_error": fatal_error,
        "total_profiles_tested": total_profiles,
        "total_failures_found": sum(int(row.get("fail_count", 0)) for row in round_rows),
        "total_new_cases_added": sum(int(row.get("new_cases_added", 0)) for row in round_rows),
        "average_profiles_per_sec_wall": round(sum(wall_speeds) / len(wall_speeds), 6) if wall_speeds else 0.0,
        "best_profiles_per_sec_wall": round(max(wall_speeds), 6) if wall_speeds else 0.0,
        "profiles_per_sec_scoring_only": round(total_profiles / score_times, 6) if score_times > 0 else 0.0,
        "cuda_available": bool(torch_status.get("cuda_available")),
        "gpu_name": torch_status.get("gpu_name"),
        "actual_evaluation_mode": "gpu_batch" if gpu_rows > 0 else mode["actual_evaluation_mode"],
        "gpu_backend_used": gpu_rows > 0,
        "gpu_rows_submitted": sum(int(row.get("gpu_rows_submitted", 0)) for row in round_rows),
        "gpu_rows_scored": gpu_rows,
        "gpu_batches_scored": sum(int(row.get("gpu_batches_scored", 0)) for row in round_rows),
        "gpu_seconds": round(sum(float(row.get("gpu_seconds", 0.0)) for row in round_rows), 6),
        "gpu_idle_estimate": round(sum(float(row.get("gpu_idle_estimate", 0.0)) for row in round_rows) / len(round_rows), 6) if round_rows else 0.0,
        "cpu_only_reason": "" if gpu_rows > 0 else mode.get("cpu_only_reason"),
        "remaining_failure_categories": dict(sorted(failure_categories.items())),
        "severity_counts": dict(sorted(severity_counts.items())),
        "top_rules_to_fix": [rule for rule, _ in rules.most_common(5)],
        "next_perf_bottleneck": str(round_rows[-1].get("next_perf_bottleneck", "")) if round_rows else "",
        "torch_installed": bool(torch_status.get("torch_installed")),
        "torch_error": torch_status.get("torch_error"),
    }


def parse_args(argv: list[str] | None = None) -> argparse.Namespace:
    parser = argparse.ArgumentParser(description="Run live adversarial optimizer battle telemetry.")
    parser.add_argument("--rounds", type=int, default=5)
    parser.add_argument("--batch-size", type=int, default=512)
    parser.add_argument("--max-generated-per-round", type=int, default=2000)
    parser.add_argument("--target-profiles-per-sec", type=float, default=20.0)
    parser.add_argument("--use-gpu", action="store_true")
    parser.add_argument("--cpu-workers", type=int, default=8)
    parser.add_argument("--dashboard", action="store_true", help="Show a Tkinter dashboard with live stats, controls, and graph.")
    parser.add_argument("--run-mode", choices=["rounds", "endless", "timed"], default="rounds")
    parser.add_argument("--minutes", type=float, default=5.0)
    parser.add_argument("--live", action="store_true")
    parser.add_argument("--debug-routing", action="store_true")
    parser.add_argument("--skip-baseline", action="store_true")
    return parser.parse_args(argv)


def run(args: argparse.Namespace, dashboard: Any | None = None, control: RunControl | None = None) -> int:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    LIVE_ROUNDS.unlink(missing_ok=True)

    control = control or RunControl(args)
    torch_status = _torch_status()
    mode = _evaluation_mode(args, torch_status)
    _print_startup(torch_status, mode)

    tool_python = battle._python_for_subprocesses()
    training_module = battle._load_training_module()
    round_rows: list[dict[str, Any]] = []

    round_no = 0
    while control.should_start_round(round_no, int(args.rounds)):
        round_no += 1
        round_started = time.perf_counter()
        training_run = (
            {"command": "skip-baseline", "returncode": 0, "elapsed_seconds": 0.0, "output_tail": ""}
            if args.skip_baseline
            else battle._run_training_subprocess(tool_python)
        )
        training_summary = battle._read_json(battle.TRAINING_SUMMARY)
        stored_cases = battle._read_jsonl(battle.ADVERSARIAL_CASES)
        seed_cases = battle._load_seed_cases(training_module)
        active_categories = battle._active_failure_categories(training_summary, stored_cases)
        battle_cases, cpu_generation_time = battle._generate_battle_cases(
            seed_cases,
            active_categories,
            round_no,
            max(0, int(args.max_generated_per_round)),
            max(1, int(args.cpu_workers)),
        )
        battle_cases = _apply_live_adaptation(
            battle_cases,
            _load_learning_state(),
            max(0, int(args.max_generated_per_round)),
        )

        if args.use_gpu:
            results, scoring_time, gpu_stats = battle._evaluate_cases_batched(
                battle_cases,
                max(1, int(args.batch_size)),
                max(1, int(args.cpu_workers)),
                True,
            )
        else:
            results, scoring_time = _evaluate_cases_with_tool_python(
                battle_cases,
                max(1, int(args.batch_size)),
                max(1, int(args.cpu_workers)),
                tool_python,
            )
            gpu_stats = {"gpu_used": False, "gpu_rows_scored": 0, "gpu_batches_scored": 0}

        new_cases_added = battle._append_new_failure_cases(round_no, battle_cases, results)
        round_wall_time = time.perf_counter() - round_started
        payload = _round_payload(
            round_no,
            args,
            battle_cases,
            results,
            cpu_generation_time,
            scoring_time,
            round_wall_time,
            new_cases_added,
            mode,
            training_run,
            gpu_stats,
        )
        learning_state = _update_learning_state(payload, control)
        payload["learning_state"] = _learning_public_view(learning_state)
        _append_jsonl_atomic(LIVE_ROUNDS, payload)
        round_rows.append(payload)
        _atomic_write_json(LIVE_SUMMARY, _summary(args, round_rows, torch_status, mode))

        if dashboard:
            dashboard.push(payload)

        print(
            f"Round {round_no}/{_round_label(args)} | tested={payload['profiles_tested']} | "
            f"pass={payload['pass_count']} | fail={payload['fail_count']} | "
            f"wall_p/s={payload['profiles_per_sec_wall']} | score_p/s={payload['profiles_per_sec_scoring_only']} | "
            f"gpu_rows={payload['gpu_rows_scored']} | gpu_idle={payload['gpu_idle_estimate']} | "
            f"new_cases={payload['new_cases_added']} | top_fail={payload['top_fail']} | "
            f"bottleneck={payload['next_perf_bottleneck']}",
            flush=True,
        )

        if control.stop_now.is_set() or control.stop_after_round.is_set():
            print("stop requested; saved summary and ending cleanly", flush=True)
            break

    _atomic_write_json(LIVE_SUMMARY, _summary(args, round_rows, torch_status, mode))
    if dashboard:
        dashboard.push({"__done__": True})
    return 0


def _run_dashboard_mode(args: argparse.Namespace) -> int:
    control = RunControl(args)
    dashboard = _create_dashboard(control)
    if dashboard is None:
        return run(args, None, control)

    result: dict[str, Any] = {"code": 1, "fatal_error": None}

    def worker() -> None:
        try:
            result["code"] = run(args, dashboard, control)
        except Exception as exc:
            result["fatal_error"] = f"{type(exc).__name__}: {exc}"
            existing_rounds = battle._read_jsonl(LIVE_ROUNDS)
            torch_status = _torch_status()
            mode = _evaluation_mode(args, torch_status)
            _atomic_write_json(LIVE_SUMMARY, _summary(args, existing_rounds, torch_status, mode, fatal_error=result["fatal_error"]))
            dashboard.push({"__done__": True, "fatal_error": result["fatal_error"]})

    thread = threading.Thread(target=worker, name="live-battle-worker", daemon=True)
    thread.start()
    dashboard.start(thread)
    control.request_stop_after_round()
    thread.join(timeout=10.0)
    return int(result.get("code", 1) or 0)


def main(argv: list[str] | None = None) -> int:
    raw_argv = sys.argv[1:] if argv is None else argv
    if raw_argv and raw_argv[0] == "--evaluate-cases-only":
        input_path = Path(raw_argv[1])
        output_path = Path(raw_argv[2])
        child_parser = argparse.ArgumentParser()
        child_parser.add_argument("--batch-size", type=int, default=512)
        child_parser.add_argument("--cpu-workers", type=int, default=8)
        child_args = child_parser.parse_args(raw_argv[3:])
        cases = battle._read_jsonl(input_path)
        results, scoring_time = battle._evaluate_cases(
            cases,
            max(1, int(child_args.batch_size)),
            max(1, int(child_args.cpu_workers)),
        )
        _atomic_write_json(output_path, {"results": results, "scoring_time": scoring_time})
        return 0

    args = parse_args(argv)
    if args.debug_routing:
        return _debug_routing(args)

    torch_status = _torch_status()
    mode = _evaluation_mode(args, torch_status)
    try:
        if args.dashboard:
            return _run_dashboard_mode(args)
        return run(args)
    except Exception as exc:
        fatal = f"{type(exc).__name__}: {exc}"
        existing_rounds = battle._read_jsonl(LIVE_ROUNDS)
        _atomic_write_json(LIVE_SUMMARY, _summary(args, existing_rounds, torch_status, mode, fatal_error=fatal))
        print(f"fatal_error: {fatal}", flush=True)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
